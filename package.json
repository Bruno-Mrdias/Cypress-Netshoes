{
  "name": "cypress-netshoes",
  "version": "1.0.0",
  "description": "",
  "main": "cypress.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "checkout": "npx cypress run --env grep=@checkout",
    "cpf": "npx cypress run --env grep=@cpf",
    "category": "npx cypress run --env grep=@category",
    "generate:report": "npx mochawesome-merge results/mochawesome/*.json | npx mochawesome-report-generator -o results/mochawesome/html",
    "cloud": "npx cypress run --record --key 09ba169d-d26b-4daf-8e8e-fee77a14870c"
  },
  "keywords": [],
  "author": "Bruno Matheus Ribeiro Dias da Silva",
  "license": "ISC",
  "devDependencies": {
    "cypress-grep": "^3.0.4",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-multi-reporters": "^2.0.4",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
